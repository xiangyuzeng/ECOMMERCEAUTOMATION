Scan the inputs/ directory for all .xlsx and .csv files. Classify each file by filename pattern per CLAUDE.md rules. Print what was found and what's missing. Then execute the full pipeline: parse all data → build keyword library → build competitor matrix → build gap analysis → build pricing model → generate the 8-tab Excel workbook → generate JSON for dashboard → generate run_summary.md. Run: `python3 scripts/generate_report.py` — if it doesn't exist yet, build it first following CLAUDE.md specs, then run it. $ARGUMENTS
